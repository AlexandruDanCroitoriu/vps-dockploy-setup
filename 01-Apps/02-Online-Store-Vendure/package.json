{
  "name": "online-store-mi-novia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,storefront -c blue,magenta \"npm run dev -w server\" \"npm run dev -w storefront\"",
    "dev:server": "npm run dev -w server",
    "dev:storefront": "npm run dev -w storefront",
    "dev:storefront-test": "npm run dev -w storefront-test",
    "build": "npm run build -w server && npm run build -w storefront && npm run build -w storefront-test",
    "build:server": "npm run build -w server",
    "build:storefront": "npm run build -w storefront",
    "build:storefront-test": "npm run build -w storefront-test",
    "start": "concurrently -n server,storefront -c blue,magenta \"npm run start -w server\" \"npm run start -w storefront\" \"npm run start -w storefront-test\"",
    "start:server": "npm run start -w server",
    "start:storefront": "npm run start -w storefront",
    "start:storefront-test": "npm run start -w storefront-test",
    "docker:up": "cd apps/server && docker compose up -d postgres_db dbgate"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "workspaces": [
    "apps/*"
  ]
}
