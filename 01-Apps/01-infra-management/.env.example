# The username entered on the login page.
ADMIN_USERNAME=admin

# Generate an escaped bcrypt hash for a Next.js .env file:
# node -e 'console.log(require("bcryptjs").hashSync("replace-with-a-strong-password", 12).replaceAll("$", "\\$"))'
ADMIN_PASSWORD_HASH=replace-with-the-generated-bcrypt-hash

# Generate with: openssl rand -base64 32
AUTH_SECRET=replace-with-a-random-secret

# Public URL of this application. Use http://localhost:3000 for local development
# and the external HTTPS address when deployed through Dokploy.
NEXTAUTH_URL=http://localhost:3000

# The URL of the Dokploy instance to which this application connects.
DOKPLOY_URL=https://dokploy.example.com

# The API key generated for this application in the Dokploy instance.
DOKPLOY_API_KEY=your-generated-api-key

# Repository scanned by the Add application dropdown. These defaults match this
# monorepo. GITHUB_TOKEN is only required when the repository is private.
GITHUB_REPOSITORY_OWNER=AlexandruDanCroitoriu
GITHUB_REPOSITORY_NAME=vps-dockploy-setup
GITHUB_REPOSITORY_BRANCH=main
GITHUB_APPS_PATH=01-Apps
# GITHUB_TOKEN=github-token-with-read-only-repository-contents-access
