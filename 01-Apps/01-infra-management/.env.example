ADMIN_USERNAME=admin        # what you type at login
ADMIN_PASSWORD_HASH=admin   # verifies the password you type
AUTH_SECRET=admin           # Auth.js uses this internally you never type it
# generate AUTH_SECRET using -> openssl rand -base64 32
